publishing {
    publications {
        APIPublication(MavenPublication) {
            from components.java
            groupId GROUP
            artifactId 'Robobinding-helpers-api'
            version VERSION
        }
    }
}